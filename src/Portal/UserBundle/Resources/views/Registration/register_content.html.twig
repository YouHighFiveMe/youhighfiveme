<form id="form" action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
    {{ form_widget(form) }}
    <div class="btns">
        <a class="button" onclick="document.getElementById('form').submit()" href="#">{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}</a>
    </div>
</form>
